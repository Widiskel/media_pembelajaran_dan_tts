name: media_pembelajaran_dan_tts
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: ^4.6.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets: 
    - assets/img/
  uses-material-design: true

window_size: 
  git: 
    url: git://github.com/google/flutter-desktop-embedding.git
    path: plugins/window_size
